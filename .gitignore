__pycache__/
*.pyc
.venv/
.pytest_cache/
.env

# Cache parquet (rigenerabili dalla CI, non servono in git)
data/*.parquet

# Output residui di run precedenti
data/*_centrali.*
data/centrali_targets.json

# Snapshot storici (prodotti dalla CI)
data/history/
